function myFunction(p1, p2, symbol) {
	if (symbol == '-')
    return p1 - p2;  
	if (symbol == '+')
    return p1 + p2;  
	if (symbol == '/')
    return p1 / p2;  
	if (symbol == '*')
    return p1 * p2;  
else return "wat";
// The function returns the product of p1 and p2
}