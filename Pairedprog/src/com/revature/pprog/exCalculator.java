package com.revature.pprog;

import java.util.Scanner;

//
//Tad was my partner for this
//Jose Luis Munoz


public class exCalculator  {

	public static void main(String[] args) throws Calexception{
/*
		Scanner scan = new Scanner(System.in);

		System.out.println("Please enter 1st number\n");
		String firstValue = scan.next();  
		
		System.out.println("Please enter 2nd number\n");
		String secondValue = scan.next(); 
		
		System.out.println("Please enter an Operator\n (+, -, *, /)");
		String operation = scan.next(); 
		
		
	try {
		Integer.parseInt(firstValue);
		Integer.parseInt(secondValue);
//		if(this is not a number) {} 
//		throw new Calexception("My exception says: \n");
//I have not added any logic to the if before my custom exception so it is not being thrown
//I used NUmberformatException as a placeholder 
	}
	catch (NumberFormatException e){
	    System.out.println(e.getMessage());
		System.out.println("not parsable");
		System.exit(0);
	}

		
		System.out.println("Type 1 for Int or 2 for Double:  ");
		int choice = scan.nextInt();
		
		if(choice==1) {
		genPractice <Integer> type1 = new genPractice<>();
		type1.setT(Integer.parseInt(firstValue));
		genPractice <Integer> type2 = new genPractice<>();
		type2.setT(Integer.parseInt(secondValue));
		
		
	
		if(operation.equals("+")) {
			System.out.println(type1.getT() + type2.getT());
		}
		if(operation.equals("-")) {
			System.out.println(type1.getT() - type2.getT());
		}
		if(operation.equals("*")) {
			System.out.println(type1.getT() * type2.getT());
		}
		if(operation.equals("/")) {
			System.out.println(type1.getT() / type2.getT());
		}
		
		}
		if(choice==2) {
		genPractice <Double> type1 = new genPractice<>();
		type1.setT(Double.parseDouble(firstValue));
		genPractice <Double> type2 = new genPractice<>();
		type2.setT(Double.parseDouble(secondValue));
		
		
		
		if(operation.equals("+")) {
			System.out.println(type1.getT() + type2.getT());
		}
		if(operation.equals("-")) {
			System.out.println(type1.getT() - type2.getT());
		}
		if(operation.equals("*")) {
			System.out.println(type1.getT() * type2.getT());
		}
		if(operation.equals("/")) {
			System.out.println(type1.getT() / type2.getT());
		}
		}
		scan.close();
	
	*/
	}	
}